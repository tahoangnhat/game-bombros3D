public enum CellType
{
    Empty,              // Sàn trống (floor)
    BorderWall,         // Tường viền
    MiddleWall,         // Tường giữa (indestructible)
    DestructibleWall    // Tường có thể phá (vật cản)
}
